const capitalize= require('./capitalize')

test('',()=>{
    expect(capitalize("yunus")).toBe("Yunus");
})

test('',()=>{
    expect(capitalize("kelly")).toBe("Kelly");
})

test('',()=>{
    expect(capitalize("ushindi")).toBe("Ushindi");
})